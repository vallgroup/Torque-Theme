version: "3.3"

services:
  301nc-db:
    image: mysql:5.7
    volumes:
      - 301nc-db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_DATABASE: torque-wp
      MYSQL_ROOT_PASSWORD_FILE: /run/secrets/torque_wp_mysql_root
      MYSQL_USER_FILE: /run/secrets/torque_wp_mysql_user
      MYSQL_PASSWORD_FILE: /run/secrets/torque_wp_mysql_pass
    networks:
      - 301nc-back
    secrets:
      - torque_wp_mysql_root
      - torque_wp_mysql_user
      - torque_wp_mysql_pass

  301nc-phpmyadmin:
    depends_on:
      - 301nc-db
    image: phpmyadmin/phpmyadmin
    restart: always
    ports:
      - 8080:80
    environment:
      PMA_HOST: 301nc-db
      MYSQL_ROOT_PASSWORD_FILE: /run/secrets/torque_wp_mysql_root
    networks:
      - 301nc-back
    secrets:
      - torque_wp_mysql_root

  301nc-wordpress:
    depends_on:
      - 301nc-db
    image: wordpress:latest
    ports:
      - "8000:80"
    volumes:
      - ./wp-content:/var/www/html/wp-content
      - ./php.ini:/var/www/html/php.ini
    restart: always
    environment:
      WORDPRESS_301nc-db_HOST: 301nc-db:3306
      WORDPRESS_301nc-db_NAME: torque-wp
      WORDPRESS_301nc-db_USER_FILE: /run/secrets/torque_wp_mysql_user
      WORDPRESS_301nc-db_PASSWORD_FILE: /run/secrets/torque_wp_mysql_pass
      WORDPRESS_DEBUG: 1
    networks:
      - 301nc-back
    secrets:
      - torque_wp_mysql_user
      - torque_wp_mysql_pass

networks:
  301nc-back:

volumes:
  301nc-db_data:
    driver: local

secrets:
  torque_wp_mysql_root:
    file: ./secrets/mysql_root.txt
  torque_wp_mysql_user:
    file: ./secrets/mysql_user.txt
  torque_wp_mysql_pass:
    file: ./secrets/mysql_pass.txt
